#! /usr/bin/bash sh

# Exit in case of error
set -e

echo "No start script"

#echo "RELOAD is $RELOAD"
#echo "HOST is $HOST"
#echo "PORT is $PORT"

#if [ -n "$RELOAD" ] && [ $RELOAD = 'true' ]; then
#    uvicorn app.main:app --host $HOST --port $PORT --reload
#else
#    gunicorn "app.main:app" --config "/proj/gunicorn_conf.py" --worker-class "uvicorn.workers.UvicornWorker"
#fi

